# Anthropic API key, used for synthetic data generation (src/data/generate_pairs.py)
# and for the LLM-as-judge (src/eval/judge.py).
ANTHROPIC_API_KEY=

# Hugging Face token, needed to download the base model (Qwen2.5-3B-Instruct
# or Llama-3.2-3B-Instruct) and optionally to push results.
HF_TOKEN=
